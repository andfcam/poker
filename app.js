window.onload = () => {
    const game = new Game();
}

// Deal 5 random cards to the table

// Blinds:
//      Player to left plays small blind
//      Player to THEIR left plays big blind

// Turn:
//      Fold, Call, Raise

// Until all players (inc. big blind) have folded or called.
window.onload = () => {
    const deck = new Deck();
}

// Create DOM space for cards
// Create deck of cards
// Shuffle deck of cards
// Assign 2 cards to each player
// Assign 5 random cards to the table